import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NgxMailSenderComponent } from './ngx-mail-sender.component';

describe('NgxMailSenderComponent', () => {
  let component: NgxMailSenderComponent;
  let fixture: ComponentFixture<NgxMailSenderComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [NgxMailSenderComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(NgxMailSenderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
